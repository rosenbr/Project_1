<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Lobster+Two&display=swap" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.gstatic.com">
    <link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>Recipe</title>
</head>
<body>

        <div class="main-navigation">
            <div class="navbar">
                <h2>Chefly</h2>
                <a class="home__button" href="/">Home</a>
                <a class="login__button" href="/login">Login</a>
                <a class="signup__items" href="/register">Signup</a>
            </div>
        </div>
    

    <div class="container">
        <div class="row recipe__top">
                <section class="col recipe__showPage">
                    <p class="recipe__name"><%= Recipes.name %></p>
                    
                </section>
                <section class="col">
                    <div class="recipe__imagePage">
                        <img src="<%= Recipes.img %>">
                    </div>
                </section>
        </div>
    </div>

    <div class="container">
        <div class="recipe__bottom">
            <h1 class="descriptions">Description
                <p class="descriptions__descriptions"><%= Recipes.descriptions %></p>
            </h1>
            <h1 class="ingredients">Ingredients
            <p class="ingredients__ingredients"><%= Recipes.ingredients %></p>
            </h1>
            <h1 class="directions">Directions
                <p class="directions__directions"> <%= Recipes.directions %></p>
            <h1>
        </div>
    </div>

    <form action="/comments/createComments/<%= Recipes.id %>" method="POST">
        <input type="text" name="body" placeholder="comment">
        <input type="submit" value="Comment">
    </form>
    <section class="comment" id="comments"> 
        
        <% Recipes.comments.forEach(comment => { %>
        <div>
            <p><%= comment.body %> </p> 
            <p><%= comment.timestamps %></p>

            <form action="/comments/showComments/<%= comment._id %>/<%= Recipes.id %>?_method=PUT" method="POST">
                <input type="text" name="body" placeholder="Edit Comment Here">
                <input type="submit" value="Edit Comment" />
            </form>

            <form action="/comments/showComments/<%= comment._id %>/<%= Recipes.id %>?_method=DELETE" method="POST">
                <input type="submit" value="Delete Comment" />
            </form>
        </div>
   <% }) %>
</section>

</body>
</html>